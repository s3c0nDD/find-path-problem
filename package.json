{
  "name": "find-path-problem",
  "version": "0.1.0",
  "description": "Little script checking if path beetween given points exists",
  "main": "index.js",
  "scripts": {
    "babel-node": "babel-node --presets=env",
    "start": "nodemon --exec npm run babel-node -- ./index.js",
    "build": "babel src -d dist"
  },
  "author": "s3c0nDD",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "nodemon": "^1.12.1"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
