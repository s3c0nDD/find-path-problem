{
  "name": "find-path-problem",
  "version": "0.1.0",
  "description": "Little script checking if path beetween given points exists",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/babel index.js | node",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "s3c0nDD",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0"
  }
}
